# Claude A Session Migration — 2026-08-13 (B)

## Repository status at close
- HEAD: `c5b1971`
- `origin/main`: matches exactly
- `git status`: clean, nothing local-only
- `WORKSTATE.yaml`: updated (this session's work + governance adoption)
- `SESSION_BOOTSTRAP.md`: updated (Rules 7 & 8 added)
- This migration document: complete
- Native-validation status: 5 questions queued, not yet relayed (see below)
- Blockers: none technical; awaiting native relay

## Completed work (Claude A, linguistic authority)

1. **Counting-pipeline audit** (`docs/CLAUDE_A_COUNTING_QA_20260813.md`,
   commit `b89e26b`). Cross-checked ~350 counting keys (1–20, all counting
   nouns) in `compiled_dict.json` against `master_dictionary.json` source
   status. **Verification scope (Rule 7):** checked all 1–20 keys for 22
   nouns (dog, fish, person, teacher, student, house, tree, car, cat, apple,
   banana, rice, water, food, road, river, mountain, village, book, fruit,
   coin, pen) against `master_dictionary.json` only — did **not** check
   `corrections.json`, `phrase_maps.js`, or `category_index.json` for
   independent copies of the same fabricated values (see Rule 8 note below).
   Found 146 clean keys, one isolated `pickPrimary` tie-break issue
   (`one dog`), and ~230 keys across 12 nouns shipping already-SUPERSEDED
   fabrications with no VERIFIED alternative anywhere in `master_dictionary.json`.

2. **Wrote native relay into corpus** (commit `b8a7408`, rebased onto
   `381bc29`). 120 new VERIFIED/HIGH entries for teacher/mountain/village/
   road/banana/car (6 nouns × 1–20), plus one `bunch of bananas` entry —
   built from the count=1 forms recorded in
   `docs/CLAUDE_A_SESSION_MIGRATION_20260813.md`. Resolves 5 of the 10 open
   classifier questions and the teacher half of the person/student/teacher
   root conflict.
   **Verification scope (Rule 7):** ran `prepare-data.js` (rebuilt
   `compiled_dict.json`, `compiled_dict_alternates.json`,
   `category_index.json`), spot-checked 15 of the 120 new keys' actual
   compiled output directly (not just build success), ran
   `test-dictionary.js` (8151/8151), `repository-intelligence.js` (PASSED
   after allowlisting 120 keys in `known_dictionary_conflicts.json`), and
   the full unit suite (203/203). **Did not** spot-check `corrections.json`
   or `phrase_maps.js` for pre-existing independent copies of the 6 nouns'
   old SUPERSEDED values — see Rule 8 gap below.

3. **Logged Project Owner clarification on `bol`** (commit `305cb12`,
   notes-only). `bol` (car classifier) is not an orthographic coincidence
   with tree root `Bol` — same word, reused as a general vehicle classifier
   (car/bike/cycle/ship, per Project Owner, exact scope still open).

4. **Governance adoption** (commit `c5b1971`, this session's final commit).
   Added Rules 7 (state verification scope) and 8 (duplicate-representation
   check before declaring resolved) to `SESSION_BOOTSTRAP.md`, per Project
   Owner directive following Claude C's independent QA findings.
   Documentation-only; no code or dictionary data touched.

## Rule 8 gap — this session's own work is provisional, not fully resolved

Per the rule just adopted, applied retroactively to this session's own
output: **neither the counting-pipeline audit nor the 120-entry relay
performed a duplicate-representation sweep.** Specifically not checked:
- `corrections.json` — may hold independent (possibly stale) copies of
  teacher/mountain/village/road/banana/car counting phrases.
- `phrase_maps.js` — same risk, runtime phrase-lookup table separate from
  `compiled_dict.json`.
- `category_index.json` — regenerated by `prepare-data.js` this session,
  but not manually diffed for correctness beyond "build succeeded."
- The other 12 flagged-broken nouns from the audit that were **not**
  written this session (house/rice/water/food, ~110 keys) — unaudited for
  duplicate representations since no fix was attempted for them yet.

**Recommended next step:** run a Rule 8 sweep across `corrections.json` and
`phrase_maps.js` for all 6 nouns fixed this session before treating that fix
as fully resolved.

## Native-validation questions queued (not yet relayed to Thangseng)

1. Student's root — is there a distinct male/female form, what is it?
2. Person's root — confirm `man·de sak·sa` = "one person" sounds natural.
3. Coin's root — confirm `Tangka·bisil gong·sa` = "one coin" sounds natural.
4. House/rice/water/food — do these take a classifier at all when counted,
   or is the number said directly? (Water/rice/food likely mass nouns —
   let the answer take whatever natural form it takes rather than forcing
   the 1–20 template.)
5. `bol` vehicle-classifier scope — does it extend to motorcycle, train,
   airplane? Or only car/bike/cycle/ship as already stated?

## Runtime Handoff (Claude B)
- All 6 nouns fixed this session (teacher/mountain/village/road/banana/car):
  VERIFIED and confirmed live in `compiled_dict.json` (spot-checked 15/120
  keys directly, all correct).
  Runtime status: **compiled_dict.json confirmed. corrections.json and
  phrase_maps.js NOT checked — see Rule 8 gap above.**
  Action: sweep `corrections.json` / `phrase_maps.js` for stale
  independent copies of these 6 nouns' old SUPERSEDED counting values.
- The remaining ~110 keys (house/rice/water/food) from the original audit:
  still shipping SUPERSEDED fabrications in `compiled_dict.json`, unchanged
  this session, no VERIFIED alternative exists yet.
  Runtime status: unresolved, unchanged from `docs/CLAUDE_A_COUNTING_QA_20260813.md`.
  Action: none until native input on question 4 above is received.
- Compile-pipeline defect flagged in the original audit (ships SUPERSEDED
  values when no VERIFIED candidate exists; `one dog`-class tie-break
  non-determinism): still open, not this session's scope (Claude B).

## Exact next step
Start a new conversation, paste this document in. Either:
(a) relay the 5 queued questions to Thangseng and write the answers in, or
(b) run the Rule 8 duplicate-representation sweep on this session's own
    6-noun fix before doing anything else, per the newly-adopted governance
    rule.
