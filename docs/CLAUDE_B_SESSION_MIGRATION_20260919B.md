# Claude B Session Migration — 2026-09-19B

## 1. Project identity
Lean Garo — English↔Garo translation engine. `translate()` in `src/translationEngine.js` is the runtime entry point; compiles from `master_dictionary.json` (not `pending_lexicon.json`, which is staging-only for the OCR review pipeline).

## 2. Current state
- **HEAD:** `353b0bd` on `origin/main`, pushed and verified clean (`git status` empty, `origin/main` confirmed at this exact commit via fetch).
- **Gate at push time:** 441/441 unit tests, 0 repository-intelligence violations (all 8 checks), 0 resync candidates, dictionary compile clean — 8813/8813 entries valid, 0 runtime errors across all 5 gate steps (prepare-data.js, test-dictionary.js, repository-intelligence.js, resync-stale-overrides.mjs, unit tests), each explicitly exit-code-verified this session close.
- **Remote auth:** `origin` remote URL has the session's GitHub PAT embedded (`https://github_pat_...@github.com/pzrjv4sfj5-prog/Lean-Garo-.git`), confirmed working for push (used successfully this session). **Security note carried forward: this PAT was pasted in plaintext in chat at session start — flagged to the Owner then to revoke/rotate after use; still applies.**
- Session history at this point: `7baa4df` (prior Claude B close) → `c738ee7` (this session's own start-of-session doc, 20260919) → `a712a48` (Claude D: dance cleanup) → `7b6edcd` (Claude D: P24/P25 OCR batch, ready_for_a) → `f9a2b5c` (Claude A: promote 192 pending-lexicon entries) → `28b990e` (Claude A: import+promote Claude D's P24/P25 batch, 84 entries) → `353b0bd` (this session, Claude B: restore "to " prefix on 27 verbs).

## 3. What's done this session
- **Investigated (not fixed, correctly deferred):** the 3 pos-only `pending_lexicon` near-dupe pairs flagged last session (PL-0002116..119, PL-0002182/183). Found the proposed "quick" fix (`pos:["v.","n."]`) would silently null the POS field — `prepare-data.js:213` only accepts `pos` as a string. Left untouched: this is a Garo-content call (one entry, two senses, vs. two real dictionary facts), explicitly flagged in the prior session's own doc as needing Claude A/Owner sign-off, not something to resolve unilaterally.
- **Race condition, resolved correctly:** independently prepared a schema-fix on `docs/CLAUDE_D_20260918_p24_p25_ready_for_a.json` (pos array→string, restore "to " prefix on 27 genuine verb infinitives, pull 2 cross-reference notes out of `entries[]`) while Claude A was simultaneously promoting the *original, unfixed* file directly into `master_dictionary.json`/`pending_lexicon.json`. My staging-file fix landed after theirs and was moot — re-diagnosed against the actual promoted data and re-applied the real fix there instead. Claude A had independently caught and better-solved the homograph half of the same problem (renamed `cane`→`cane (verb)`, `flower`→`flower (verb)`, `skin`→`skin (verb)`, `smell`→`smell (verb, perceive)` as distinct keys, rather than my weaker english+garo pair-matching) — kept their approach, only added the "to " prefix on top of it.
- **Fixed:** root-caused and fixed why Project Owner's "neutralize `to X` → `X`, doesn't change meaning" instruction, while correct on meaning, would have broken sentence-building for the 27 words it touched: `lookupEngine.js`'s `VERB_LEMMAS` — the *only* verb-detection signal `sentenceBuilder.js` uses at runtime — is built by scanning `compiled_dict.json` keys for a literal `"to "` prefix. Independently confirmed `pos` does not survive `compiled_dict.json` compilation for plain-string entries (8540+/8542 of them), corroborated by `translationEngine.js`'s own `getAllVocabulary()` 2026-08-09 bugfix comment making the identical observation for a different function. Restored `"to "` on all 27 in both `master_dictionary.json` and `pending_lexicon.json` (kept in sync); `prepare-data.js`'s existing bare-infinitive-alias step regenerates the neutralized bare form automatically, so the Owner's actual intent (both `"forbid"` and `"to forbid"` resolve) is fully preserved — fix is source-side only, zero functionality lost.
- **Found in the process (not a bug I introduced):** restoring `"to crumble down"` collided case-insensitively with a pre-existing, unrelated `"To crumble down"` → `Gropa` entry — invisible until the prefix was restored, since bare `"crumble down"` hadn't collided with anything. Not my call which Garo form is correct (or whether both are genuine coexisting variants) — allowlisted in `known_dictionary_conflicts.json` per the exact precedent Claude A set today for the batch's other 10 coexisting-variant pairs, not resolved to a single form.
- **Live-verified, not just gate-green:** `VERB_LEMMAS.has()` true for all 27 restored words including the 4 parenthetical sense-tagged ones (lemma extraction's `split('(')[0]` correctly strips `" (verb)"` etc.). Ran `translate()` on ~24 real sentences across the 27 words spanning multiple persons/tenses/negation — 20 resolved via `grammar-assembly`/`sov-assembly` (verb correctly identified, not passthrough); confirmed several correctly deferred to pre-existing higher-confidence entries (VERIFIED or `corrections.json`) over the new unverified P24/P25 forms, exactly as the confidence hierarchy is designed to do.
- **Found, not fixed (separate, pre-existing gaps — flagged to the person this session, not investigated further):** `"she estranged her family"`, `"he skins the animal"`, `"the wall crumbles down"`, `"it crumbled down yesterday"` all returned `[UNKNOWN]` fragments (morphology fallback, 0.65) — appear to be vocabulary-coverage gaps (`"wall"`, `"animal"` as object nouns) and/or object-argument sentence-assembly issues, unrelated to the `VERB_LEMMAS` fix (parallel constructions without the problem object, e.g. `"do not estrange him"`, `"i will skin the fish"`, `"they crumble down"`, all worked correctly).
- **Process note, caught before any damage:** mid-session, a `git commit` failed silently (missing git author identity — error text was easy to miss), then a subsequent `git commit --amend` — intended to retry that failed commit — instead amended Claude D's actual prior commit (7b6edcd) since no new commit existed to amend. Caught immediately via `git log`/`git reflog` before pushing; `git reset --hard` to the real 7b6edcd, redone cleanly as a fresh commit. **Nothing bad ever reached `origin`** — confirmed via `git fetch` + `git log origin/main` before and after. Lesson for next session: verify `git commit` actually succeeded (check the returned commit hash, not just absence of an obvious error) before any subsequent `--amend`.

## 4. Held / open items (not touched this session)
- **3 pending_lexicon near-dupe pairs** (PL-0002116/117, PL-0002118/119, PL-0002182/183) — unchanged from last session, still needs Claude A/Owner call on merge-vs-dual-entry. See prior migration doc (`CLAUDE_B_SESSION_MIGRATION_20260919.md` §4) for full detail; not re-litigated here.
- **94 near-duplicate rows** from Claude D's audit batch (predates this session) — still unresolved.
- `se` 20-99 compound — zero evidence, no guess exists. (`jol` 20-99 compound: Owner confirmed this decision is now closed, no longer pending — not re-litigated.)
- `DERIVED` confidence-schema gap — `VALID_CONFIDENCE_VALUES` enum still missing the tag.
- **New, this session:** `pos`-as-array is now a confirmed *recurring* bug pattern in OCR-batch handoffs (seen in both last session's pending_lexicon pairs and this session's P24/P25 batch) — worth a real `prepare-data.js`/import-pipeline fix (coerce or reject array `pos` explicitly, rather than silently nulling it) if it surfaces a third time, rather than continuing to catch it by hand per-batch.
- **New, this session:** the 4 sentence-building gaps in §3 (`estrange`+object, `skin`+"animal", `"wall"` as subject/object, `"crumbled down"` past-tense-with-adverbial) — not investigated past initial discovery.

## 5. Standing rules this session followed (carried forward)
- Never fabricate a Garo linguistic form without citation; decline (honest `[UNKNOWN]`) over a confident guess.
- Mechanical/engineering fixes (schema coercion, verb-detection wiring, git hygiene) are in-scope without owner sign-off; Garo-form/content decisions (which word is correct, how to merge two senses) are not — deferred every time this session, even when a "quick" mechanical-looking fix was floated for one.
- Before pushing: full gate re-run at the exact commit being pushed, not just at the commit written locally — this session's fetch-before-push checks caught Claude A's concurrent promotion twice (once triggering the reset-and-redo in §3).
- Commit messages document root cause, not just the change, and explicitly flag anything left open for the next session/agent — including this session's own git-identity mistake, logged rather than quietly cleaned up.
- Token discipline: matched the person's own priority list from `<userPreferences>` throughout — didn't re-verify settled findings (e.g., the `VERB_LEMMAS` root-cause diagnosis was done once, then reused directly against the live data without redoing the investigation), didn't restate prior turns' content back to the person.

## 6. Exact next step
No in-progress edit — last action was a clean push (`353b0bd`), gate green, verified zero runtime errors across all 5 gate steps. Next session should pick from §4, most actionable: either the recurring `pos`-array bug (a real pipeline fix, in-scope, no sign-off needed) or the 4 sentence-building gaps found this session (`estrange`+object, `"animal"`/`"wall"` vocabulary coverage) if there's appetite to dig into sentence-assembly rather than dictionary content.

## 7. Resume protocol for whoever picks this up
Treat this doc as ground truth for what happened, but re-sync with actual current state before continuing: `git fetch`, check `origin/main` HEAD against `353b0bd`, re-run the full gate at whatever HEAD actually is (not just trust this doc), then proceed. Don't re-litigate the fixes or open items listed above as settled — they're settled — but do verify nothing new landed between this doc and resume time (this session itself hit exactly that race twice with Claude A's concurrent work; check `git log HEAD..origin/main` isn't just a formality).

**PAT note:** if resuming in a fresh session, the person will need to provide credentials again (this session's PAT is chat-scoped, not persisted) — flag the plaintext-paste security note from §2 again if so.
