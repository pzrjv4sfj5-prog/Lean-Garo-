# Claude B Session Migration — 2026-09-20C

## 1. Project identity
Lean Garo — English↔Garo translation engine. `translate()` in `src/translationEngine.js` is the runtime entry point; compiles from `master_dictionary.json` (not `pending_lexicon.json`, which is staging-only for the OCR review pipeline).

## 2. Current state
- **HEAD:** `f437199` on `origin/main`, pushed and verified clean (`git status` empty, `origin/main` confirmed at this exact commit via fetch).
- **Gate at push time:** 456/456 unit tests, 0 repository-intelligence violations (all 8 checks), 0 resync candidates, dictionary compile clean — 8824/8824 entries valid, `npm run build` clean (same pre-existing chunk-size advisory only), lint at the same 9 pre-existing errors (all in `prepare-data.js`/`src/research/*.js`, none in any file touched this session), `server.js` smoke test clean (`GET /` → 200, no console errors). All 5 gate steps plus build/lint/smoke explicitly re-verified at this exact HEAD before this close.
- **Remote auth:** PAT shared directly in chat, reused across every push this session per explicit instruction. Used only for `git fetch`/`git push`, stripped from the local remote URL (`git remote set-url origin` back to the plain HTTPS URL) immediately after every single push — verified via `grep -c github_pat_ .git/config` returning `0` each time, never persisted anywhere in the repo. **Security note carried forward: this PAT was pasted in plaintext in chat — flagged to the Owner to revoke/rotate after use; still applies.**
- Session history at this point: `d50a4c0` (Claude A: uko vs Biko adjudication, unblocked this session's first fix) → `19e619e` (this session: her/it accusative-object fix) → `584de97` (Claude A: market=bajal, concurrent) → `b158512` (this session: pos-as-array pipeline fix) → `4aee478` (Claude A: skin override + Ritchasa gni, concurrent) → `f5ab725` (this session: §6.2 Thangseng relay draft) → `5859b13` (this session: 2nd Thangseng relay batch) → `763255b`/`67615db` (Claude A: §6.2 pattern-based adjudication + correction, concurrent) → `f437199` (this session: tree/road classifier citation close).

## 3. What's done this session
- **Reinstated the her/it accusative-object fix** (§6.1 from the prior session's doc), unblocked by Claude A's same-session `uko` vs `Biko` adjudication (`d50a4c0`). `grammarEngine.js`'s object-extraction loop now resolves sentence-final "her"/"it" to `uko` instead of silently dropping them. Two collisions caught live during verification and fixed: a separate possessive-scan loop was independently double-tagging "her" as `Uni` alongside the correct `uko`; and `uko` itself needed the same double-accusative-marker exemption already established for `Angko`, to avoid producing `uko·ko`. Commit `19e619e`, 8 new regression tests (`tests/unit/her_it_accusative_object.test.js`).
- **Fixed the recurring `pos`-as-array silent-null bug** flagged across two prior sessions (docs/CLAUDE_B_SESSION_MIGRATION_20260919B.md §4). `prepare-data.js` now coerces the confirmed real OCR-batch shape (a single-element string array, e.g. `["v."]`) instead of silently discarding any array shape to `null` with zero trace, and visibly reports anything more ambiguous (empty array, 2+ elements) instead of guessing at it — new `docs/POS_ARRAY_COERCIONS.md` report. Caught and fixed one false positive during this fix's own gate run: an early comment wording tripped `repository-intelligence.js` Check D's guard against `prepare-data.js` ever referencing `pending_lexicon.json` (a prose mention, not actual code). Commits `b158512` (`62b785c` locally, rebased), 6 new tests (`tests/unit/pos_array_coercion.test.js`).
- **Drafted two Thangseng relay batches** (not sent — Project Owner/Tridip to relay per standing process):
  - `docs/THANGSENG_RELAY_QUESTION_20260920.md` — him/her/it/us/them/you-all as accusative objects. While drafting, found a real competing citation the earlier grep-based investigation missed: `master_dictionary.json` idx 8454, `"Us. (in the accusative case)."` → `An·chingko`, a distinct root from the currently-shipping `Chingna` (derives from `An·ching`=inclusive-we, not `Chinga`=exclusive-we) — the same two-candidate-root shape as the just-closed `uko`/`Biko` question. Deliberately doesn't name any candidate form in the sent section, so the reply reflects Thangseng's own sentence rather than confirming/denying a supplied guess.
  - `docs/THANGSENG_RELAY_QUESTION_20260920B.md` — 10 short, plain sentences (no linguistic jargon, per the person's request to keep it quick to answer and easy to process) covering 4 whole grammar categories with zero native confirmation on record (relative clauses, comparative/superlative, plural marking, passive voice) plus 2 pronoun-case forms (`angna`/`ango`, "for me"/"with me") marked pattern-inferred-only in `docs/GRAMMAR_CONFIDENCE_MATRIX.md`. Self-corrected one item before finalizing: an earlier draft's 10th question (an animate/people counting classifier) turned out to already be ✅ confirmed in that same matrix (`sak·`, `mande sak·sa`) — replaced with the one classifier actually still unconfirmed there, `brong·` (long objects).
- **Declined to generalize §6.2** (him/us/them's unconditional `·ko` suffix) without a citation — searched again this session, found none beyond what was already known, left held per the project's own standing rule against guessing at Garo grammar. Claude A independently reached a compatible pattern-based (explicitly non-citation) call the same session for `him`/`them` specifically (`763255b`, refined `67615db` after seeing this session's `An·chingko` find) — see `pending_handoff_from_claude_a_20260920B` in `.ai/WORKSTATE.yaml` for the full reasoning; `us` correctly still routed to the drafted relay question rather than inferred.
- **Investigated the 4 sentence-building gaps** flagged in the prior session's doc (`estrange`+object, `skin`+"animal", `"wall"` as subject/object, `"crumbled down"`) and reclassified them: on closer look they're vocabulary-coverage/engineering issues (missing `"wall"`/`"animal"` as object nouns, a suspected object-argument assembly bug), not Thangseng questions. Not yet fixed — correctly not bundled into either relay batch, since sending an engineering question to a native-speaker relay would waste the ask.
- **Closed two small classifier-citation items** via a live relay in chat: `'tree'→pang` confirmed via `"panggni"` = "two trees" (had zero citation before this — added a proper comment, no behavior change, since the pre-existing default already matched the now-confirmed fact). Separately corrected a mischaracterization from earlier the same session (chat only, not a prior commit): `'road'→dil` was described as an untested default in this session's own classifier-status summary, which was wrong — it already carried a real citation (`"Rama dilsa"`, Owner directive, 2026-09-10); the code itself was always correct, only the chat summary was mistaken. Commit `f437199`, both verified byte-identical live before/after (`panggni`, `dilsa`).
- **`.ai/WORKSTATE.yaml` updated** this close: old `claude_b.next_action` (2026-09-17) renamed to `next_action_prior_20260917`, fresh `next_action` written summarizing this session. Validated the file still parses as YAML after the edit (`python3 -c "import yaml; yaml.safe_load(...)"`) before committing, given this file's own documented history of duplicate-key/parse-fragility issues.

## 4. Held / open items (not touched this session)
- **§6.2**, `him`/`us`/`them`'s `·ko` suffix — partially addressed (Claude A's pattern-inference for `him`/`them`, see above), `us` still genuinely open pending the drafted relay reply.
- **The 4 sentence-building gaps** — now correctly reclassified as engineering/vocabulary issues, not linguistic ones, but still not fixed. Next actionable step if picked up: check whether `"wall"` and `"animal"` are genuinely absent from `master_dictionary.json` as object-position nouns, and isolate the suspected object-argument sentence-assembly bug with a minimal repro (parallel constructions without the problem object already work, per the original finding — `"do not estrange him"`, `"i will skin the fish"`, `"they crumble down"` all resolved correctly).
- **Both Thangseng relay batches** (`_20260920.md`, `_20260920B.md`) — drafted, not sent. Sending is Project Owner/Tridip's action, outside this role.
- **94 near-duplicate rows** from Claude D's audit batch — still unresolved, predates this session, not touched.
- Everything else already flagged as open in prior migration docs and not mentioned above is unchanged in substance — not re-litigated here.

## 5. Standing rules this session followed (carried forward)
- Never fabricate a Garo linguistic form without citation; decline (honest `[UNKNOWN]`, or a held-open item) over a confident guess — applied explicitly to §6.2 and to not naming candidate forms in either drafted relay question.
- Mechanical/engineering fixes (schema coercion, object-loop disambiguation, git hygiene, classifier-citation bookkeeping) are in-scope without owner sign-off; Garo-form/content decisions are not.
- Before pushing: full gate re-run at the exact commit being pushed, not just at the commit written locally — this session hit concurrent Claude A drift 4 separate times and rebased+re-gated clean each time before pushing.
- Commit messages document root cause, not just the change, and explicitly flag anything left open.
- Self-corrected in-band, twice, rather than letting an error stand: the classifier-question item that was already-confirmed and shouldn't have been asked (`sak·`/people), and the `road`/`dil` mischaracterization in this session's own chat summary — both caught and fixed same session, not carried forward silently.
- Token discipline: matched the person's own stated priorities throughout — no filler, no restating requests, led with results, didn't re-verify settled findings unnecessarily.

## 6. Exact next step
No in-progress edit — last action was a clean push (`f437199`), gate green, zero runtime errors across all 5 gate steps plus build/lint/smoke. Next session should pick from §4: most actionable is the 4 sentence-building gaps' vocabulary-coverage half (check if `"wall"`/`"animal"` are real dictionary gaps), since that's engineering-scope and doesn't need to wait on either relay reply.

## 7. Resume protocol for whoever picks this up
Treat this doc as ground truth for what happened, but re-sync with actual current state before continuing: `git fetch`, check `origin/main` HEAD against `f437199`, run the full gate at whatever HEAD actually is (not just trust this doc), then proceed. Don't re-litigate the fixes or open items listed above as settled — they're settled — but do verify nothing new landed between this doc and resume time (this session itself hit exactly that race 4 times with Claude A's concurrent work; checking `git log HEAD..origin/main` is not a formality here).

## 8. PAT usage for next session
This session had no git remote access until the person pasted a GitHub PAT directly in chat. That token:
- Is **not** stored anywhere in this repo, this doc, or persisted by Claude between sessions — it lived only in that chat's conversation history.
- Was used exactly like this to authenticate: `git remote set-url origin https://<PAT>@github.com/pzrjv4sfj5-prog/Lean-Garo-.git`, then `git push`/`git fetch` as normal, then immediately `git remote set-url origin https://github.com/pzrjv4sfj5-prog/Lean-Garo-.git` to scrub it back out — repeated for every single push this session, verified clean each time via `grep -c github_pat_ .git/config`.
- **The next Claude B session will need a fresh PAT from the person** — either the same one re-pasted (if not yet revoked) or a newly rotated one. There is no way to resume git access from this doc alone.
- Restated again: that token was pasted in plaintext in chat. Flag to the person again on resume that it should be revoked/rotated after use.

## 9. Link to this doc
Pinned to the exact commit this session ended on (not a moving `main` reference, so it can't silently drift if something else merges before resume):
`https://github.com/pzrjv4sfj5-prog/Lean-Garo-/blob/f437199/docs/CLAUDE_B_SESSION_MIGRATION_20260920C.md`
