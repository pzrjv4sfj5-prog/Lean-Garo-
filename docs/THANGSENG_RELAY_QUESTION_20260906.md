# Thangseng Relay Questions — 2026-09-06 (session 20260906E)

Evidence-first: none of these are guessed or resolved by pattern —
each needs an explicit answer.

~~1. `get` vs `can` collision.~~ **CLOSED, NV-148 (2026-09-07):** both
confirmed correct, genuine polysemy — no distinguishing cue given.

2. **`two`: `gni` vs `gnisan`. PARTIALLY CLOSED, NV-149/NV-150
   (2026-09-07):** `gini` was a spelling error — confirmed `two`/`2` is
   `gni` only, matching the classifier-suffix use. Still open, not
   addressed: how does `gnisan` (NV-138) relate to `gni` — is it a
   suffixed/citation form of the same root, or a different word?

3. **Tied candidate — "she will not be able to finish the difficult
   work tomorrow."** Two renderings on file, no signal for which is
   correct:
   - `Ua man·a·chi knal·ko man·jawa`
   - `rakbegipa kamko ua knalo matchotatna man·jawa`

   Which of these is the correct/natural rendering — or are both valid
   with a difference in register/emphasis?

4. **`re·anga`: "went" vs "to leave".** New relay (NV-145): `re·anga` =
   "to leave" (infinitive). But `re·anga` is already `verified_high`
   for "went" (NV-100, distinct from "go" = `re·a`). Is `re·anga`
   genuinely doing double duty for a shared departure concept ("went"
   and "to leave"), or was one of these mishearing/misrelay of a
   different tensed form?

5. **`donbo`/`re·anga` for "leave": possible imperative/infinitive
   mood pattern?** NV-146's "leave me alone" uses `donbo` (imperative)
   for "leave", distinct from NV-145's `re·anga` (infinitive, "to
   leave"). This looks structurally parallel to NV-147's confirmed
   `poraibo`/`poraia` (imperative/statement) pattern for "study" — does
   "leave" follow the same mood split, with `donbo` as the imperative
   counterpart to `re·anga`?

~~6. `saksan` vs `ak·sa` for "alone".~~ **CLOSED, NV-151 (2026-09-07):**
   confirmed synonyms. Still open, not addressed: whether `saksan` also
   covers "loneliness" (separate unverified row on file).

Not yet sent to Thangseng. Hold for the next relay batch alongside any
other queued items (see `.ai/WORKSTATE.yaml` `pending_thangseng_questions`).
