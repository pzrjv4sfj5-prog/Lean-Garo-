# Thangseng Relay — Single Question (2026-08-31C)

**Status:** Not yet sent. For Project Owner to relay via Tridip/WhatsApp.
**Source:** Joint Claude A + Claude B grammar/morphology/tense audit,
`docs/CLAUDE_A_GRAMMAR_MORPHOLOGY_TENSE_AUDIT_20260831.md` §3,
`.ai/WORKSTATE.yaml` `claude_a.pending_thangseng_questions`.

---

## Question: "can" — `ama` vs `man·a`

We already have full confirmation for "can" (ability) = `ama`:
- "I can eat" = *Anga cha'na ama*
- "I can go" = *Anga re'angna ama*
- "I can work" = *Anga kam ka'na ama*

This is **not** what we're asking about — that paradigm is settled.

We separately have a second, less-attested word on record for "can":
`man·a`. We don't know how it relates to `ama`.

**Question for Thangseng:**

> Are `ama` and `man·a` the same word (just a spelling or dialect
> variant), or are they different? For example, could one be used for
> physical/learned ability and the other for permission — or is it a
> difference in formality/register? If they're different, could you
> give one example sentence for each, so the difference is clear?

**Why we're asking:** so we can correctly document `man·a` in the
dictionary (as a duplicate of `ama`, or as its own distinct entry with
its own meaning) without guessing.

**Does not block other work:** the engine team can go ahead and
implement the `ama` "can" construction now — that part is already
fully confirmed and doesn't depend on this answer.
