repository:
  head: de6918f
  last_updated: "2026-07-25"
  test_status: "106/106 regression tests passing, build clean"
  bootstrap_entry_point: ".ai/SESSION_BOOTSTRAP.md"
  governance_note: >-
    2026-07-09, Project Owner directive: Repository Access Model in
    SESSION_BOOTSTRAP.md was REPLACED, not amended. Claude A may now
    push directly to origin/main in any session where the Project Owner
    explicitly supplies a temporary PAT for that session, subject to the
    7-step discipline documented there (sync, review commits, check for
    duplicate work, do the work, test, sync docs, then push - same
    commit, not a follow-up). Without a PAT, Claude A has no write
    access and falls back to the git-format-patch relay pattern, which
    remains available. This supersedes the previous "Claude A never has
    push access, ever" policy referenced in CLAUDE_A_FINAL_HANDOUT.md;
    that file is left unedited as historical record per its own stated
    precedence rule (SESSION_BOOTSTRAP.md wins on conflict). Role
    boundaries (Claude A = linguistic authority, Claude B = engineering/
    repository architecture) are unchanged - this is an access-workflow
    change only.
  note: >-
    CLAUDE_A_FINAL_HANDOUT.md (repo root) added e0cea23 - closing note
    from the original Claude A instance, applied via git format-patch
    relay per the access model that was then current (see governance_note
    above for the 2026-07-09 update). A "Current joint work package"
    section was added to SESSION_BOOTSTRAP.md the same session - both
    roles should check it first for active priorities rather than
    relying solely on the per-role sections below, which may lag it
    slightly.

claude_a:
  status: active
  migration_doc: >-
    docs/CLAUDE_A_SESSION_MIGRATION_20260715.md - superseded by the
    2026-07-24 migration doc pasted directly into the current session
    (not yet its own committed file). See current_task below for
    everything since.
  current_task: >-
    2026-07-25: RULE-041 fix - "To work" master_dictionary.json entry
    had garo/pos fields swapped by OCR import (Kam ka·a was stuck in
    pos, not garo), root cause of the "he works" bug Claude B flagged;
    corrected + synced pending_lexicon.json PL-0001397 (12da508).
    NV-040 opened+closed same session - direct Thangseng confirmation
    of fruit=Bite, correcting 564295d's commit message which implied
    native confirmation but was actually a mechanical dedup (d95d731).
    NV-039 fully closed for housefly=Tampi as its own headword; broader
    fly noun 3-way conflict (Bila/Tampi/til·a) intentionally left open
    (de6918f). Compiled a 42-question, 17-topic consolidated pending
    batch for the next full Thangseng relay round (not yet committed
    to repo - currently only in
    /mnt/user-data/outputs/THANGSENG_QUESTION_BATCH_20260725.md,
    see next_action). Audited own prior work: found
    PENDING_REGRESSION_CASES.md stale on RC-CANDIDATE-017 (says "Open,
    unimplemented", actually fixed+tested in 66cac87) - not yet
    corrected in that file. Verified Claude D's page 2/30 "redundant
    commit" flag was a false alarm (both are the legitimate promoted-
    work audit trail, not duplicate pushes) - no repo action needed.
  progress: >-
    This WORKSTATE.yaml itself had drifted 9 days / 2 sessions behind
    actual repo state before this update - HEAD, test count, and this
    entire claude_a block were still showing 2026-07-16. Root cause:
    no session closed out by updating this file, even when other repo
    docs (GRAMMAR_RULE_CATALOGUE.md, THANGSENG_NATIVE_VALIDATION.md)
    were kept current. Treat this as a standing checklist item going
    forward, not a one-off catch-up.
  completed:
    - "docs/GRAMMAR_SPECIFICATION.md, MORPHOLOGY_SPECIFICATION.md, GRAMMAR_RULE_CATALOGUE.md (RULE-001..RULE-041), VALIDATION_CORPUS.md - core V1.0 linguistic deliverables"
    - "2026-07-08: RULE-034/035 locative review; docs/THANGSENG_NATIVE_VALIDATION.md created (NV-001..009)"
    - "2026-07-16: RC-CANDIDATE-017/018/019 reviewed and reconciled (see PENDING_REGRESSION_CASES.md history)"
    - "2026-07-20..24: NV-013/025/028/035/036/037 closed; page 2 processed solo (Claude D token-exhaustion stopgap) and promoted, RC-CANDIDATE-028 logged"
    - "2026-07-25: RULE-041 root-cause fix; NV-039 (housefly) and NV-040 (fruit) closed; 42-question consolidated Thangseng batch compiled"
  next_action: >-
    1) Commit the 42-question batch into docs/ as the canonical
    pending-questions file (currently only sitting in chat output, per
    Project Owner - not yet done, should not be left un-committed
    indefinitely). 2) Correct RC-CANDIDATE-017's stale "Open,
    unimplemented" status line in PENDING_REGRESSION_CASES.md to match
    reality (fixed in 66cac87). 3) Update this file at the end of every
    session going forward, not just when directly asked.
  do_not_repeat:
    - "Do not re-derive the suffix paradigm table from scratch - final in MORPHOLOGY_SPECIFICATION.md §3 unless native validation changes it"
    - "Do not re-audit raka consistency across corrections.json - exhaustively done already, majority-vote method established"
    - "Do not re-litigate Gemini-fallback-removal - settled, architectural, not open"
    - "Do not re-review the locative proposal - closed 2026-07-08, see RULE-034/035"
    - "Do not create new PENDING_NATIVE_QUESTIONS_* files - use docs/THANGSENG_NATIVE_VALIDATION.md, update questions in place"
    - "Do not attempt to resolve Rule 30/31 from assumptions - both explicitly need direct Thangseng validation, see NV-001/NV-002"
    - "Do not begin Verb Family Project with re·/re·ang - start with cha· per Project Owner sequencing, re·/re·ang is second, after NV-001"
    - "Do not re-run the coverage audit or confidence review from scratch - both are dated 2026-07-08, re-run only after new corpus rows are added or new native validation lands"
    - "Do not re-triage RC-CANDIDATE-027 case-key instances individually as they're found - log the count, batch-handle with Claude B"
    - "Do not let this WORKSTATE.yaml go stale again - update at session close, every session"
  waiting_for: >-
    Thangseng answers to the 42-question consolidated batch (see
    next_action #1). Claude B on RULE-040 ("right" 3-way engine-side
    split) and RC-CANDIDATE-027 (case-key clobber, ~470 pairs,
    scoping-only so far). Claude D on pages 34/74 (duplicate
    resolution, blocked until actual files are pushed) and 75-77/94/
    95/100/101 (not yet started).
  expected_output: "Markdown/JSON files committed to docs/ and dictionary data, per role boundary - Claude A never touches engine code (Claude B) or does OCR ingestion (Claude D)"

claude_b:
  status: active
  current_task: >-
    Convergence protocol adopted (2026-07-11, Project Owner): applying
    it retroactively found RC-CANDIDATE-010 - RC-002's ·o locative fix
    was implemented more generally (any noun after in/on/at) than
    RULE-G2's own stated confidence supports (High for one confirmed
    example, Medium/not-stress-tested for general productivity). Not
    reverted (strict improvement over the prior definitely-wrong ·ko),
    flagged as a linguistic feedback item for Claude A instead of
    silently left as if fully validated. See docs/
    PENDING_REGRESSION_CASES.md Pending section.
  progress: >-
    Codex audit: 5 items VERIFIED and fixed (VerbsGrammar.jsx x5,
    dead phrase_maps.js entries, classifierHints jol/ge gap), several
    more VERIFIED-but-real items catalogued (dist/ tracked, lint
    continue-on-error, root/docs ARCHITECTURE.md duplication,
    master_dictionary.json uncovered by repository-intelligence.js), 4
    REJECTED claims documented with exact reasoning (stale 51/51 claim
    not found, 3 of 6 "obsolete" files don't exist, server.js
    verified active not legacy), rest correctly FUTURE-scoped. Full
    classification in PROJECT_STATUS.md §8. Also implemented Claude
    A's approved RC-CANDIDATE-002/003/006/008(5-of-9) fix directives
    (d0e6c06), then correctly REVERTED the 'bought' fix when Claude A's
    own verdict was retracted upstream (df71a83) - verification
    discipline applied reflexively, not just to Codex. Restructured
    PENDING_REGRESSION_CASES.md into Implemented/Pending sections per
    explicit instruction (cd60e4d). 61/61 tests passing, build clean.
  completed:
    - "Grammar-modeling audit: real past-tense suffix logic, dedup, dead code removal (aeec036)"
    - "docs/GRAMMAR_SPEC.md living rule index created (84bf88a)"
    - "Rules 32-33 (search=Sandia, down=Ka·ma) integrated + duplicate-contamination fixes (5f0d02e, a65e936)"
    - "Repository Architect pass: SEO, CI workflow, a11y/UX, dead-code cleanup (0cab1b5)"
    - "docs/ARCHITECTURE.md complete technical reference (d416f07)"
    - "V1.0 launch sprint: README consistency fixes, P2 dead-code removal (dcf6293)"
    - "Fixed under/Ka·ma·o lexical confusion, verified down=Ka·ma consistency (edc94b7, 1b64b0c)"
    - "Production verification audit: no hidden regressions from recent fixes"
    - ".ai/SESSION_BOOTSTRAP.md created; docs/PENDING_LINGUISTIC_PROPOSAL_20260708_locatives.md logged"
    - "Native Sentence Validation Audit Case 1 (native-confirmed gloss + morpheme breakdown), PENDING_VOCABULARY.md, PENDING_REGRESSION_CASES.md"
    - "Applied CLAUDE_A_FINAL_HANDOUT.md via format-patch relay; added Repository access model + Current joint work package to SESSION_BOOTSTRAP.md"
    - "Phase 2: docs/PHASE2_TRANSLATION_INTELLIGENCE.md - decision intelligence, pipeline mapping, reverse-translation readiness, semantic integrity debt"
    - "BACKLOG-002: IRREGULAR_VERBS extracted to src/data/irregular_verbs.json, data-integrity test added, 52/52 passing"
    - "BACKLOG-001 complete: PURPOSE_MAP/PRONOUN_MAP/POSSESSIVES extracted; RC-CANDIDATE-006 (stale search purpose-form) discovered and logged, not fixed, 55/55 passing"
    - "Governance update: replaced Repository Access Model per Project Owner directive (Claude A may push directly with a session-scoped PAT)"
    - "BACKLOG-006 first increment: repository-intelligence.js (raka report + cross-table consistency gate), RC-CANDIDATE-007/008/009 logged, 56/56 passing"
  next_action: >-
    BACKLOG-006 first increment done. Per docs/REPOSITORY_
    INTELLIGENCE.md's own documented gaps: extending to master_
    dictionary.json/garo_dictionary.json needs its own design pass (not
    started); reverse raka direction not attempted (false-positive risk).
    RC-003, RC-CANDIDATE-006 through 009 all remain pending Claude A -
    not touched. Standing by for more native sentences, Claude A's
    review cycle, or the next approved engineering item.
  do_not_repeat:
    - "Do not re-derive raka (Rule 1) violations from scratch - check docs/ARCHITECTURE.md §9 audit history first"
    - "Do not re-add PROGRESSIVE_MAP/PAST_TO_ROOT - confirmed dead, removed dcf6293"
    - "Do not re-implement jaha as past-negation - superseded, see Rule 17/27"
    - "Do not implement chat-sourced linguistic proposals directly - must go through Claude A -> docs/ commit first (standing integration rule as of 2026-07-08)"
  waiting_for: "Claude A to review docs/PENDING_LINGUISTIC_PROPOSAL_20260708_locatives.md and commit canonical rule(s); Thangseng native validation on Rules 30/31"
  expected_output: "Repository fully bootstrap-able from .ai/ + PROJECT_STATUS.md with zero conversational context required"

chatgpt:
  status: unknown
  current_task: unknown
  progress: unknown
  completed: []
  next_action: unknown
  do_not_repeat: []
